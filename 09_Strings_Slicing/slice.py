fruit= "Mango"
MangoLen = len(fruit) #len fuction gives the length of string
print(MangoLen)

print(fruit[0:4])# including 0 but not 4 i.e it will gives chracter from 0 to 3
print(fruit[1:4])# including 1 but not 4 i.e it will gives chracter from 1 to 3
print(fruit[:4]) # this is same as fruit[0:4] it automatically adds 0
print(fruit[:]) # this will add the length of string after colon(:) i.e 5 in this case
print(fruit[:5]) # this is same as above